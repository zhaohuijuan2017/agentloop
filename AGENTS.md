# AgentLoop / LoopForge 协作约定

## 默认文档语言

- 本项目下的文档默认使用中文书写，包括 SPEC、设计说明、评审记录、README、交付报告和项目规则。
- 代码契约类内容保持原文，不做中文化：代码标识符、类型名、字段名、枚举值、API 路径、错误码、命令、脚本名、JSON 示例和日志。
- 文档正文应以中文叙述为主；英文只用于上述代码契约或业内固定术语。

## 基线记录

- 项目基线使用 git 提交记录表达，不使用未提交工作区状态作为基线。
- 每个阶段或任务开工前，先确认当前基线提交；完成后用新的提交记录交付范围与验证结果。
- 不把他人的未提交 WIP 混入自己的基线或交付提交。

详细规则见 `docs/rules/project-rules.md`。
